export default function Approvals() {
  return (
    <section className="page">
      <h2>Approvals</h2>
      <div className="placeholder">
        Pending approval queue. Approvers can review invoices, approve, or reject with
        comments before invoices are sent to clients.
      </div>
    </section>
  );
}

export default function Invoices() {
  return (
    <section className="page">
      <h2>Invoices</h2>
      <div className="placeholder">
        Create, edit, and send invoices. Connect to <code>GET /invoices</code> when the
        backend endpoint is implemented.
      </div>
    </section>
  );
}
